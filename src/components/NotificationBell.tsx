import React from 'react';

export default function NotificationBell(): JSX.Element {
  return (
    <div className="notification-bell" aria-hidden="true">
      {/* Placeholder: notification bell temporarily simplified to remove corrupted git-diff text. */}
    </div>
  );
}
