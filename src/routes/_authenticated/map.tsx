import React from 'react';

export default function MapRoute(): JSX.Element {
  return (
    <div className="min-h-screen grid place-items-center">
      <div className="text-center">
        <div className="text-lg font-semibold">Map</div>
        <p className="mt-2 text-sm text-muted-foreground">Placeholder page — restored corrupted file to remove git-diff metadata that broke the build. Re-add the original map UI from git history if needed.</p>
      </div>
    </div>
  );
}
